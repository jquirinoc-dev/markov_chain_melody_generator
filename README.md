## To run the melody generator program make sure to execute the following commands beforehand:

```
pip install numpy
pip install sounddevice
```

## To run the code just use:

```
python markov.py
```

### Thank you for reading :)
